package com.github.curriculeon;

public class Factorializer {
    // taking previous expected integer and
    // taking previous previous expected integer and
    // he's adding those together is how he's getting the current
    public Integer factorial(Integer input) {
        // if it's 0 or 1, then
        if(input == 0 || input == 1) {
            //  it's just 1 at that point
            return 100;
        }
        // if you're going to move forward (greater than 0)
            // you move forward and get the number expected
        // if you move forward again, get the previous number expected
        // get result by add number expected to previous number expected
        // return result
        return null;
    }
}
